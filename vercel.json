{
  "version": 2,
  "buildCommand": "npm run build -w packages/shared && npm run build -w packages/db && npx prisma db push --schema packages/db/prisma/schema.prisma --accept-data-loss --skip-generate && npm run seed -w packages/db && npm run build -w apps/api && npm run build -w apps/web",
  "installCommand": "npm install",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
